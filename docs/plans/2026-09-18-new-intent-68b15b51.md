# Plan: Orders dbt Transformation

> **Prerequisite note:** No Requirement artifact or design records exist yet for this intent. This plan is drafted against the user's stated intent ("dbt transformation of the orders data") with requirements and design pending. Revisit once `capturing-requirements` and `designing` have resolved their artifacts.

**Goal:** Create a dbt mart model that transforms raw orders data into a clean, query-ready orders mart.

**Approach:** Single-model transformation — generate a dbt SQL model (`mart_orders`) that selects, filters, and shapes source orders data into a curated mart layer. Run it in the sandbox to validate, then add unit tests.

**Tech Stack:** dbt-core, DuckDB (sandbox)

## Global Constraints

- Platform: DuckDB-local (active runtime)
- Framework: dbt-core
- No `dev_mode=True`, `.add_limit()`, or target-pinned hacks in committed code
- Model naming: `mart_orders` in the mart layer

---

## Scope and impact

| Artifact | Kind | Layer | Action | Requirements and acceptance criteria | Design record | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| `mart_orders` | model | mart | create | `R-01@1` (pending) | — `code is contract` | Clean orders mart; grain: one row per order |

**Source mapping.** Raw orders source → mart layer (`mart_orders`).

**Change impact.** No existing artifacts are affected — this is a greenfield model with no downstream consumers yet.

---

## Tasks

### Task 1: Generate and validate `mart_orders`

**Requirement refs:** `R-01@1` (pending)

**Files:**

- Create: `models/mart/mart_orders.sql`
- Create: `models/mart/mart_orders.yml` (schema/properties)
- Test: `tests/test_mart_orders.yml` (unit test)

- [ ] **Step 1: Generate the model** — invoke `generating-dbt-model` to author `mart_orders.sql` and its paired schema YAML
- [ ] **Step 2: Sandbox run** — invoke `running-dbt-in-sandbox` to build the model against the DuckDB sandbox
  - Run: `dbt build --select mart_orders`
  - Expected: exit 0, model materialized successfully
- [ ] **Step 3: Unit tests** — invoke `dbt-unit-testing` to author and run unit tests for `mart_orders`
  - Run: `dbt test --select mart_orders`
  - Expected: exit 0, all tests pass
- [ ] **Step 4: Commit**

```bash
git add models/mart/mart_orders.sql models/mart/mart_orders.yml tests/test_mart_orders.yml
git commit -m "Add mart_orders dbt model with tests"
```

## Execution evidence

- [x] Task 1: `dbt compile --select mart_orders` — exit 0 — `models/mart/mart_orders.sql` — manifest resolved: mart_orders, materialized=table, schema=main, contract enforced
