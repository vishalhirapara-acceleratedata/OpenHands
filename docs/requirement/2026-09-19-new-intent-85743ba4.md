---
kinds: [transformation]
---

# Intent: fct_sales dbt model from orders

## Classification

| Axis | Value | Rationale |
| --- | --- | --- |
| Action | `work` | New dbt model requested |
| Kinds | `[transformation]` | A new `fct_sales` model is a new relation — product change in the transformation kind |

## Goal

Create a `fct_sales` dbt fact model built from an `orders` source, providing a curated sales fact table for downstream consumption.

## Source system

- **orders** — named as the source for `fct_sales`. No source connection, schema, or table definition has been configured or profiled yet.

## Target

- Platform: DuckDB-local (per runtime configuration)
- Schema: `main` (per runtime configuration)

## Deliverables inventory

| # | Deliverable | Kind | Requirement refs | Notes |
| --- | --- | --- | --- | --- |
| 1 | `fct_sales` | model | R-01@1 | New dbt fact model built from orders source |

## Requirements

| ID | Revision | Requirement | Acceptance criteria | Source | Resolution | Status |
| --- | --- | --- | --- | --- | --- | --- |
| R-01 | 1 | A dbt model named `fct_sales` exists, built from the `orders` source | Model compiles and runs in sandbox; materializes in the target schema | User request | supplied decision | pending |
| R-02 | 1 | `fct_sales` grain (unique key) is defined and enforced | Unique key constraint passes; no duplicate key rows in output | — | open question | pending |
| R-03 | 1 | `fct_sales` metric columns are defined with documented business rules | Each metric column has a documented formula; independent recomputation from source matches | — | open question | pending |
| R-04 | 1 | Null handling for metrics and key columns is defined | Null behavior matches documented rule; no unexpected nulls in key columns | — | open question | pending |
| R-05 | 1 | Consumers and refresh cadence are documented | Refresh schedule is configured or documented as on-demand | — | open question | pending |

## Out of scope

- Nothing explicitly excluded by the user. To be confirmed once grain, metrics, and consumers are resolved.

## Open questions

| Branch | Question | Take-it-as-is? |
| --- | --- | --- |
| Grain | What is the unique key of `fct_sales`? (e.g., one row per order, one row per order line item) | No — blocks model design and test authoring |
| Metric definitions | Which metrics should `fct_sales` contain and what are their formulas? (e.g., revenue, quantity, tax, discount) | No — blocks model SQL and acceptance criteria |
| Null handling | How should nulls in key columns and metrics be handled? | No — blocks test authoring |
| Consumers | Who reads `fct_sales`? (e.g., analysts, downstream marts, dashboards) | No — affects documentation and exposure |
| Refresh cadence | What is the expected refresh schedule? (e.g., daily, hourly, on-demand) | No — affects SLA acceptance |

## Design pending

- Source table schema and availability — requires source connection configuration and profiling
- Medallion layer placement (staging vs. intermediate vs. mart) — to be resolved by Design after profiling
- Materialization strategy (table vs. view vs. incremental) — design decision
- Column naming and typing conventions — design decision

## Change history

| Date | Event | Details |
| --- | --- | --- |
| 2026-09-19 | Initial capture | Created Requirement from user request: "fct_sales dbt model built from orders". Material branches (grain, metrics, nulls, consumers, cadence) recorded as open questions after two timed-out interview attempts. |

## Change impact

No downstream artifacts, plans, or tasks exist yet. This is a new intent with no prior approved work to impact.

## Approvals

No approvals recorded. Capture is pending and held pending resolution of material business questions.
